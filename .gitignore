# --- Python / Django ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
*.sqlite3
/backend/staticfiles/
/backend/media/

# --- Node / React ---
node_modules/
/frontend/dist/
/frontend/.vite/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- OS / IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# Celery beat runtime schedule (local artifact)
backend/celerybeat-schedule*
.coverage
htmlcov/

# Knowledge-graph build artifacts (graphify)
graphify-out/

# --- Монорепозиторий миграции (uv workspace) ---
# Лок-файл коммитится: воспроизводимость сборки важнее чистоты диффа.
.mypy_cache/
.ruff_cache/
.pytest_cache/
coverage.xml

# Стабы, сгенерированные `buf generate` из contracts/*.proto — артефакт сборки.
libs/grpc_stubs/

# Локальная конфигурация dev-окружения (шаблон — deploy/compose/.env.example).
deploy/compose/.env
