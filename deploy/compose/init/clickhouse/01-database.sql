-- Базы ClickHouse. Схемы таблиц заводит timeseries-service своими миграциями
-- (фаза 3) — здесь только контейнеры для них.
--
-- CLICKHOUSE_DB из окружения создаётся образом автоматически; эта база нужна
-- отдельно, под интеграционные тесты, чтобы они не роняли рабочие данные.

CREATE DATABASE IF NOT EXISTS wb_analytics_test;
