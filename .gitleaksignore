# Разобранные находки gitleaks. Каждая строка — отпечаток, который был осмотрен
# человеком и признан не секретом. Список должен оставаться коротким: длинный
# означает, что гейт обходят, а не пользуются им.

# Синтетический ключ формы AWS внутри самопроверки детектора: скрипт
# tools/secrets-check.sh подкладывает его во временный каталог, чтобы убедиться,
# что gitleaks вообще срабатывает — иначе молчащий гейт неотличим от чистого
# репозитория. Ключ выдуман и никогда никуда не давал доступа.
#
# Находка относится к коммиту ae567dc, где литерал ещё лежал в файле целиком.
# Начиная со следующего коммита ключ собирается из двух частей в рантайме, и
# новых находок этот скрипт не порождает — но из истории литерал никуда не
# делся, а переписывать опубликованную ветку ради этого несоразмерно.
ae567dc2c5677b04158c540981f35e8525fbbb65:tools/secrets-check.sh:aws-access-token:36
