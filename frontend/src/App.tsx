/**
 * App shell placeholder. Real surfaces (query bar, filters, table, charts,
 * schedules, alerts, auth) are wired in their user-story tasks (US2+).
 */
export default function App() {
  return (
    <main>
      <h1>WB Analytics</h1>
    </main>
  );
}
