"""Domain layer — pure. No framework, no ORM, no I/O.

Imports here are limited to the standard library and other domain modules.
That constraint is what makes this layer portable, and it is enforced by
`tests/test_layering.py` rather than by good intentions.
"""
