# 8. План задач

Исполняемый бэклог. Фазы 0-1 расписаны до уровня задачи, фазы 2-6 — до уровня
рабочего пакета: планировать работу, до которой три месяца, с точностью до
файла бессмысленно.

**Обозначения**

- `[P]` — задача может выполняться параллельно с соседними `[P]` в том же блоке
- `→ Tnnn` — зависит от указанной задачи
- **DoD** — критерий готовности сверх общего из [док. 7, §7.1](07-implementation-plan.md)

---

## Фаза 0 · Фундамент

### Блок 0.A — Каркас репозитория

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T001 | Создать ветку `migration/fastapi-microservices` от `main` | — | ✅ **сделано** (от `00aa21c`) |
| T002 | Корневой `pyproject.toml` с uv-workspace (`members = ["libs/*", "services/*"]`), `uv.lock` | T001 | `uv sync` проходит на чистой машине |
| T003 | `ruff.toml`, `mypy.ini`, `.gitignore` (включая `libs/grpc_stubs/`), `.editorconfig` | T002 | `uv run ruff check .` зелёный на пустом дереве |
| T004 | Каталоги `libs/`, `contracts/`, `services/`, `deploy/`, `tools/` с `README.md` в каждом | T002 | Структура совпадает с [док. 4, §4.10](04-tech-stack.md) |

### Блок 0.B — Dev-окружение и контракты

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T005 `[P]` | `deploy/compose/docker-compose.yml`: PostgreSQL 16, Kafka 4.x KRaft (один контейнер), Schema Registry, ClickHouse, Redis, MinIO | T004 | `docker compose up -d` → все healthcheck зелёные |
| T006 `[P]` | `deploy/compose/`: Grafana + Prometheus + Loki + Tempo, преднастроенные datasource | T004 | Grafana открывается, датасорсы отвечают |
| T007 `[P]` | `contracts/buf.yaml`, `buf.gen.yaml`, пустые `events/` и `grpc/` | T004 | `buf lint` проходит |
| T008 | Скрипт `tools/dev-up.ps1` / `dev-up.sh` — поднять окружение и применить схемы | T005 | Одна команда от `git clone` до рабочего окружения |

### Блок 0.C — `libs/platform`

Каждая задача включает свои тесты. Порядок внутри блока — по зависимостям.

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T010 | `config.py` — база на Pydantic Settings, падение при неполной конфигурации | T003 | Тест: отсутствующая обязательная переменная → падение при импорте, не в рантайме |
| T011 | `logging.py` — `structlog` в JSON, контекст через contextvars | T010 | Тест: `trace_id` и `service` попадают в каждую запись без явной передачи |
| T012 | `errors.py` — иерархия ошибок, конверт `{error:{code,message,details,trace_id}}` | T011 | Тест: внутренние детали не протекают наружу ни в одном коде ответа |
| T013 | `otel.py` — бутстрап трассировки, инъекция/извлечение `traceparent` для Kafka-заголовков и gRPC-metadata | T011 | Тест сквозной трассы: producer → consumer сохраняет `trace_id` |
| T014 | `db.py` — async-engine, сессия, `statement_cache_size=0` зашит | T010 | Тест: движок нельзя создать с ненулевым кешем prepared statements |
| T015 | **`pagination.py`** — keyset-курсор, генерация цепочки условий для смешанных направлений ([док. 7, §7.5](07-implementation-plan.md)) | T012 | Property-тест: обход всех страниц даёт каждую строку ровно один раз при любой комбинации направлений |
| T016 | `outbox.py` — таблица, запись в транзакции, relay-воркер | T014 | Тест: убийство процесса между коммитом и публикацией не теряет событие |
| T017 | `idempotency.py` — `Idempotency-Key` в Redis, ключ + хеш тела + ответ | T010 | Тест: повтор с тем же ключом возвращает исходный ответ; с тем же ключом и другим телом → 409 |
| T018 | `kafka.py` — продюсер, консьюмер, дедуп по `event_id`, ручной коммит **после** обработки | T013, T017 | Тест: повторная доставка не создаёт дубля; падение в обработчике не двигает офсет |
| T019 | `grpc_.py` — серверные и клиентские интерцепторы, health-протокол, отображение ошибок в `google.rpc.Status` | T012, T013 | Тест: доменная ошибка приходит клиенту тем же кодом, что в REST |
| T020 | `auth.py` — проверка JWT RS256, JWKS-клиент с фоновым обновлением | T010 | Тест: ротация ключа не роняет проверку уже выданных токенов |
| T021 | `health.py` — `/healthz` (процесс) и `/readyz` (зависимости) | T014 | Тест: недоступная БД → `readyz` 503, `healthz` 200 |
| T022 | `testing/` — фикстуры testcontainers (PostgreSQL, ClickHouse, Kafka KRaft, Redis), один контейнер на сессию | T005 | Интеграционный тест поднимается менее чем за 30 с на прогретом кеше образов |

### Блок 0.D — Шаблон сервиса

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T030 | `tools/service-template` (cookiecutter): каркас `domain/application/adapters/composition`, `pyproject.toml`, `alembic/`, `tests/` | T010-T022 | — |
| T031 | В шаблон: `Dockerfile` multi-stage, `python:3.13-slim`, non-root, HEALTHCHECK | T030 | `trivy image` без HIGH/CRITICAL |
| T032 | В шаблон: `helm-values.yaml` под общий чарт | T030 | — |
| T033 | В шаблон: параметризованный пайплайн CI | T030 | — |
| T034 | Проверка шаблона: сгенерировать одноразовый сервис, прогнать пайплайн, удалить | T031-T033 | Сгенерированный сервис даёт зелёный пайплайн без ручных правок |

### Блок 0.E — Контракт OpenAPI из Django

Детальный разбор — [док. 5, §0.1](05-migration-plan.md). Работа идёт в
`backend/`, ни от чего в `libs/` не зависит.

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T040 | Установить `drf-spectacular`, зарегистрировать в `INSTALLED_APPS` и `DEFAULT_SCHEMA_CLASS` | T001 | `manage.py spectacular` отрабатывает (пусть и с предупреждениями) |
| T041 `[P]` | `ProductPageSerializer` — ответ `{count,next,previous,results}` | T040 | — |
| T042 `[P]` | `ParseEnqueuedSerializer` — ответ `{task_id,query,status}` | T040 | — |
| T043 `[P]` | `PriceHistorySerializer` — ответ `{wb_id,points}` | T040 | — |
| T044 `[P]` | `QueryComparisonSerializer` — ответ `{items:[{query,stats}]}` | T040 | — |
| T045 `[P]` | `ErrorSerializer` — ответ `{detail}` для 400/404/502 | T040 | — |
| T046 | `catalog/adapters/inbound/http/schema_params.py` — единый список query-параметров | T040 | — |
| T047 | **Тест на расхождение**: объявленные в `schema_params` имена совпадают с ключами, которые читают `parse_product_filter` и `parse_ordering` | T046 | Добавление параметра в парсер без объявления роняет CI |
| T048 | `@extend_schema` на вьюхи catalog (`ProductListView`, `ParseView`, `TaskStatusView`) | T041,T042,T046 | Логика вьюх не изменилась — диффом видно только аннотации |
| T049 | `@extend_schema` на вьюхи analytics (`HistoryView`, `StatsView`) | T043,T044,T046 | — |
| T050 | `@extend_schema` на `ExportView` — `OpenApiTypes.BINARY`, два content-type | T045 | — |
| T051 | `@extend_schema` на вьюхи scheduling, notifications, accounts | T045 | — |
| T052 | Сборка `contracts/openapi/v1.yaml` через `spectacular --validate --fail-on-warn` | T048-T051 | Команда проходит **без** предупреждений |
| T053 | Обвязка `schemathesis` поверх спеки, прогон против работающего Django | T052 | Ноль расхождений спеки и реализации |

### Блок 0.F — CI и алиасы

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T060 | GitHub Actions: `uv`, `ruff`, `mypy --strict`, `pytest`, покрытие ≥ 80 %, матрица по сервисам | T034 | Пайплайн зелёный на пустом дереве сервисов |
| T061 `[P]` | В CI: `pip-audit`, Trivy на образы, `gitleaks` | T060 | Подложенный тестовый секрет роняет пайплайн |
| T062 `[P]` | В CI: `buf lint` и `buf breaking` против `main` | T007 | Ломающая правка `.proto` роняет пайплайн |
| T063 | Helm-чарт-шаблон `deploy/helm/wb-service` | T032 | `helm template` даёт валидные манифесты |
| T064 | ArgoCD: приложение и автосинхронизация | T063 | — |
| T065 | Алиасы `/v1/*` к текущим путям Django, без изменения поведения | T053 | Существующие e2e-тесты проходят по обоим префиксам |
| T066 | Фронтенд переведён на `/v1` | T065 | 37 тестов Vitest зелёные |

**Выход фазы 0** — критерии в [док. 6, §6.3](06-risks.md).

---

## Фаза 1 · catalog-service на FastAPI

### Блок 1.A — Перенос ядра

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T100 | Сгенерировать `services/catalog` из шаблона | T034 | Пустой сервис отвечает на `/healthz` |
| T101 | Скопировать `catalog/domain/` **без правок** + `tests/domain/` | T100 | Тесты домена проходят в новом сервисе без единой правки |
| T102 | Скопировать `catalog/application/` **без правок** + `tests/application/` | T101 | То же для сценариев |
| T103 | **Контрольная точка R2.** Зафиксировать, потребовались ли правки | T102 | Если правки были где-либо кроме `Ordering` — стоп, пересмотр оценки |

### Блок 1.B — Многоуровневая сортировка

Обоснование — [док. 7, §7.6](07-implementation-plan.md). Единственная
санкционированная правка `application/`.

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T110 | `Ordering` → список ключей `[(field, descending), …]`; одиночный `?ordering=` трактуется как список из одного | T102 | Существующие тесты сортировки проходят без изменений |
| T111 | Валидация: поле не повторяется, все поля из `ORDERABLE_FIELDS`, длина списка ограничена | T110 | Тест на каждое правило |
| T112 | Разбор нового формата в HTTP-адаптере, обратная совместимость со старым | T111 | Оба формата дают одинаковый результат для одноуровневой сортировки |

### Блок 1.C — Персистентность

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T120 | SQLAlchemy-модели `product`, `search_query` | T100 | — |
| T121 | Alembic: начальная миграция, описывающая существующую схему | T120 | `alembic upgrade head` на копии прод-базы проходит без изменений данных |
| T122 | Миграция `wb_id` → `(marketplace, external_id)`, `marketplace` по умолчанию `'wb'` | T121 | Откат миграции восстанавливает исходную схему |
| T123 | Репозиторий: идемпотентный upsert по составному ключу | T122 | Тест: повторный upsert не создаёт дубля и не теряет `created_at` |
| T124 | **Keyset-репозиторий** — генерация цепочки условий по списку ключей сортировки | T015, T110 | Обход всех страниц выдаёт каждую строку ровно один раз для всех трёх пресетов интерфейса |
| T125 | Индексы под пресеты и одиночные сортировки; `name` — с явной collation | T124 | `EXPLAIN` показывает Index Scan для каждого пресета |
| T126 | Логирование `unindexed_sort` для комбинаций вне списка | T125 | — |
| T127 | PgBouncer в compose и в Helm, `statement_cache_size=0` | T014 | Нагрузочный тест идёт **через** PgBouncer |

### Блок 1.D — HTTP и сборка

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T130 | FastAPI-роутеры `GET /v1/products`, Pydantic-схемы ответа | T124 | Ответ побайтово совпадает с Django для одинакового запроса |
| T131 | `POST /v1/collections`, `GET /v1/collections/{id}` (пока проксируют в Django) | T130 | — |
| T132 | Отображение `InvalidFilter` и `UpstreamUnavailable` в коды 400 и 502 | T012, T130 | Тексты ошибок совпадают с текущими |
| T133 | dishka-контейнер вместо `composition/container.py` | T130 | — |
| T134 | `/healthz`, `/readyz`, `/metrics` | T021 | — |

### Блок 1.E — Вывод в прод

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T140 | Traefik: `/v1/products*` → catalog-service, остальное → Django | T130 | — |
| T141 | `schemathesis` против **обеих** реализаций, сравнение ответов | T053, T130 | Ноль расхождений |
| T142 | Нагрузочный тест `k6`: p99 не хуже Django | T127, T140 | p99 `< 150 мс` (док. 3, §3.8) |
| T143 | Канареечный вывод 5 % → 50 % → 100 % | T141, T142 | На каждой ступени — сутки без роста ошибок |

### Блок 1.F — Фронтенд

Идёт в том же спринте: серверная пагинация без правки клиента оставит таблицу
ограниченной тысячей строк.

| ID | Задача | Зависит | DoD |
|---|---|---|---|
| T150 | `TanStack Query` вместо ручных `fetch` в `api/products.ts` | T066 | Существующие тесты компонентов проходят |
| T151 | `manualPagination: true`, убрать `getPaginationRowModel` | T150, T130 | — |
| T152 | Курсор и размер страницы в `buildProductsQuery` | T151 | — |
| T153 | Многоуровневая сортировка отправляется на сервер целиком, а не первым уровнем | T112, T152 | Все три пресета дают тот же порядок, что и раньше |
| T154 | `PAGE_SIZE = 1000` убран, надпись «первые N из M» больше не нужна | T153 | Тест: выборка на 5 000 товаров листается до последней страницы |
| T155 | Обновить тесты `ProductTable.test.tsx` под серверную пагинацию | T154 | — |

**Выход фазы 1** — критерии в [док. 6, §6.3](06-risks.md), включая проверку
листания на выборке свыше 1 000 товаров.

---

## Фазы 2-6 · рабочие пакеты

Детализируются до задач при подходе к фазе.

### Фаза 2 · Шина и разрыв связей

| Пакет | Содержание |
|---|---|
| 2.1 | Kafka 4.x KRaft: 3 брокера + 3 контроллера, `kafka-storage format` в Helm-хуке, Schema Registry |
| 2.2 | Проверка совместимости `aiokafka` с Kafka 4.x |
| 2.3 | Четыре `.proto`-схемы событий, `buf` в CI |
| 2.4 | Outbox + relay в catalog-service |
| 2.5 | `scheduling` → событие вместо вызова `catalog_container` (**Б2**) |
| 2.6 | `analytics` → подписка вместо импорта `ProductModel` (**Б1**) |
| 2.7 | Kafka-адаптер вместо `InProcessEventBus`, порт не меняется (**Б3**) |
| 2.8 | Идемпотентность консьюмеров, дедуп по `event_id` |
| 2.9 | WebSocket прогресса сбора (отклонение П3) |

### Фаза 3 · ClickHouse

| Пакет | Содержание |
|---|---|
| 3.1 | `timeseries-service`, схема `price_observations` |
| 3.2 | `analytics-service`, проекция `products_current`, все агрегаты на ClickHouse |
| 3.3 | Батч-вставка снимков вместо N+1 (**H3**) |
| 3.4 | Кэш агрегатов в Redis со сбросом по событию |
| 3.5 | Эндпоинты гистограмм, удаление `lib/histogram.ts` (отклонение П1) |
| 3.6 | Двойная запись, 2 недели |
| 3.7 | Бэкфилл истории + сверка по контрольным суммам (**риск R4**) |
| 3.8 | Переключение чтения, затем отдельной выкаткой — отключение записи в PostgreSQL |

### Фаза 4 · Асинхронный сбор

| Пакет | Содержание |
|---|---|
| 4.1 | `ingestion-service` на `aiohttp`, `TCPConnector(limit_per_host)` (**H1, H2**) |
| 4.2 | Переписать тесты шлюза с `respx` на `aioresponses` (**риск R13**) |
| 4.3 | Ротация прокси на каждый запрос — то, чего не позволял `httpx` |
| 4.4 | Адаптивный лимитер, circuit breaker, `tenacity` |
| 4.5 | `Idempotency-Key` вместо `find_active(query)` |
| 4.6 | CLI-обёртка, сохраняющая поведение `parse_wb` |
| 4.7 | `scheduling-service` на `FOR UPDATE SKIP LOCKED` (**H7**) |
| 4.8 | Celery → Taskiq (**H8**) |

### Фаза 5 · Остальные сервисы и вывод Django

| Пакет | Содержание |
|---|---|
| 5.1 | `identity-service`: пароль, Google, JWT RS256 + JWKS, отзыв в Redis |
| 5.2 | Миграция пользователей, `argon2` с ленивым перехешированием (**риск R9**) |
| 5.3 | `notification-service`: правила, дедуп, доставка с dead-letter |
| 5.4 | `export-service`: потоковая генерация в S3, `202` (отклонение П2) |
| 5.5 | `api-gateway`: троттлинг, заголовки безопасности, правило публичного чтения |
| 5.6 | gRPC-интерфейсы: unary для шлюза и notifications, **server-streaming** для export |
| 5.7 | Закрытие прямого REST доменных сервисов |
| 5.8 | RLS на `saved_search`, `alert_rule`, `schedule` |
| 5.9 | Вывод Django из контура и удаление `backend/` |

### Фаза 6 · Продакшн-готовность

| Пакет | Содержание |
|---|---|
| 6.1 | HPA по RPS, KEDA по лагу Kafka |
| 6.2 | `pact` на REST-периметре, `buf breaking` как gate для gRPC |
| 6.3 | Ресурсные бюджеты и PodDisruptionBudget на девять сервисов |
| 6.4 | `k6` до SLO, gate на регресс в CI |
| 6.5 | Хаос-тесты: брокер, под, недоступность Wildberries |
| 6.6 | Дашборды RED/USE + кворум KRaft, алерты на error budget |
| 6.7 | Runbook, регламент повышения `metadata.version` (**риск R12**), on-call |
| 6.8 | Проверка отката на живом стенде |

---

## Сводка объёма

| Фаза | Задач / пакетов | Оценка |
|---|---|---|
| 0 | 34 задачи | 1-2 нед |
| 1 | 25 задач | 2-3 нед |
| 2 | 9 пакетов | 2-3 нед |
| 3 | 8 пакетов | 2-3 нед |
| 4 | 8 пакетов | 2-3 нед |
| 5 | 9 пакетов | 2-3 нед |
| 6 | 8 пакетов | 2 нед |

**Итого 11-16 недель** для команды 3-4 инженеров — совпадает с оценкой
[док. 5, §5.2](05-migration-plan.md).

Критический путь: `T002 → libs/platform → T030 шаблон → T100 catalog-service`.
Всё, что помечено `[P]`, снимается с него.
